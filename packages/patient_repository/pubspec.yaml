name: patient_repository
description: Adaptor for patient query getter. E.g. Potentially sourced through
  Hasura, AWS Amplify AppSync, Google Firebase
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  equatable: ^2.0.0
